apiVersion: v2
name: cilium
description: A Helm chart for Kubernetes

type: application
version: 0.1.0
appVersion: "1.16.0"

dependencies:
  - name: cilium
    version:  1.13.1
    repository: "https://helm.cilium.io/"