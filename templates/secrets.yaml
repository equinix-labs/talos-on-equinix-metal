env:
  METAL_AUTH_TOKEN:
  PACKET_API_KEY: same as METAL_AUTH_TOKEN
  PROJECT_ID:
  METAL_PROJECT_ID: same as PROJECT_ID
  GCP_PROJECT_ID:
  GOCY_DOMAIN:
  GCP_SA_NAME:
  GOCY_ADMIN_EMAIL:
  DOCKERHUB_USER:
  DOCKERHUB_TOKEN: