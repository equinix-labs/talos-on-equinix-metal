idp_auth:
  dex:
    staticPasswords:
      enabled: False
    bouncer_fqdn:
  role_bindings:
    - provider: # dex|okta|github
      group_prefix:
      admins:
        group_name:
      developers:
        group_name:
      audience:
        group_name:
      
env:
  METAL_AUTH_TOKEN:
  PACKET_API_KEY: # same as METAL_AUTH_TOKEN
  PROJECT_ID:
  METAL_PROJECT_ID: # same as PROJECT_ID
  GCP_PROJECT_ID:
  GOCY_DOMAIN:
  GCP_SA_NAME:
  GOCY_ADMIN_EMAIL:
  DOCKERHUB_USER:
  DOCKERHUB_TOKEN:
  KENNY_PASS:
  ERIC_PASS:
  GOCY_OAUTH_CLIENT_SECRET:
  GOCY_OAUTH_COOKIE_SECRET:
  GOCY_ARGOCD_SSO_CLIENT_SECRET:
  GOCY_KUBELOGIN_SSO_CLIENT_SECRET:
  GOCY_GH_CLIENT_ID:
  GOCY_GH_CLIENT_SECRET:
  GOCY_OIDC_CLIENT_ID:
  GOCY_OIDC_URL: